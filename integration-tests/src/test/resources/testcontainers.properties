# TestContainers configuration
# Set to true to disable TestContainers if Docker is not available
testcontainers.reuse.enable=false
ryuk.container.timeout=120

# Uncomment to skip tests if Docker is not available
# testcontainers.disabled=true